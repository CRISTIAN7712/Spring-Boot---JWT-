spring.application.name=jwt

# --- Servidor ---
server.port=8080

# --- Configuración personalizada JWT ---
# Se recomienda usar una cadena Hex o Base64 de al menos 256 bits (32 caracteres).
application.security.jwt.secret-key= 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000

# --- Base de Datos (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/spring_jwt
spring.datasource.username=postgres
spring.datasource.password=210896

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# El dialecto ya no es obligatorio en Spring Boot 3 (lo detecta automático),
# pero si quieres dejarlo explícito, es esta línea:
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
